# 开发Taro踩到过哪些坑？

### 1.css冲突

> 问题：开发H5时，不同页面如果有相同的class名，可能会出现样式冲突的问题，而小程序不会。

> 原因：小程序页面的wxss仅作用于当前页面，而 H5 是编译成了css文件，每加载一个新页面，都会将该页面的css文件全局引入，导致样式可能冲突。

> 解决方法：使用 `cssModules` 或者 每个页面组件的最上层容器以 page-PageName 的格式命名 class，并在scss文件中将该 class 置于顶层，嵌套子集的 class