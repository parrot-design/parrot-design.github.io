# 对vue和react的理解，说一下他们的异同

相同之处：

1. 都使用了虚拟DOM，提高重绘性能
2. 都有props的概念，允许组件间传递数据
3. 都有组件化的思想，将应用拆分成一个个功能明确的模块，提高复用性

不同之处：

1. 数据流

Vue默认支持数据双向绑定，而React不支持双向绑定，一直提倡单向数据流。

2. 使用上

React更接近于写原生JS代码，比如循环、判断等都是自己写的，但是vue中给你提供了模板。

3. 虚拟DOM

Vue中，由于它在渲染过程中，会跟踪每一个组件的依赖关系，当应用状态发生变化时，不需要重新渲染整个组件库。