# process是什么

process是一个全局变量，提供当前node进程信息并对其进行控制。

进程是一个基本单位，是线程的容器

当我们启动了一个js文件，实际上就是开启了一个服务进程，每个进程都拥有自己的独立空间地址，数据栈，像另一个进程无法访问当前进程的变量、数据结构，只有数据通信后，进程之间才可以数据共享

由于JavaScript是一个单线程语言，所以通过node xxx启动一个文件后，只有一条主线程